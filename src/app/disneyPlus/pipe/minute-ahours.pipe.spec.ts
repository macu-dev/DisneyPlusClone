import { MinuteAHoursPipe } from './minute-ahours.pipe';

describe('MinuteAHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new MinuteAHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
